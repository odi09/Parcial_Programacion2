/*
 * informe.c
 *
 *  Created on: 19 oct. 2021
 *      Author: ojose
 */
#include <stdio.h>
#include <stdlib.h>
#include "string.h"
#include <ctype.h>
#include "utn.h"
#include "salon.h"
#include "arcade.h"

imprimirTodo(eSalon impSalon, int largoSala, eArcade impAcarde, int largoArcade)
{
 int retorno;
 int i;
 /*
 if(impSalon != NULL && impAcarde != NULL, largoSala > 0 && largoArcade > 0)
 {
	for(i = 0; i < largoSala; i++)
	{
		if(impAcarde[i].isEmpty == 1 && impSalon[i].isEmpty == 1)
		{
			printf("%d_Nacionalidad del Arade: %s - Tipo de Sonido: %d - Nombre del Juego: %s - Cantidad de Jugadores: %d - Cantidad Maxima de Fichas: %d - Id: %d - Prerteneciente a la Sala: %s\n\n"
					,contador, impAcarde[i].nacionadidad, impAcarde[i].tipoSonido, impAcarde[i].nombreJuego, impAcarde[i].cantiJugadores, impAcarde[i].cantiMaxFichas, impAcarde[i].id, largoSala[i].);
							retorno = 1;
		}
	}


 }*/

 return retorno;
}
